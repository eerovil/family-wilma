node_modules/
dist/

# Secrets — Wilma passwords, API keys, OAuth credentials.
.env
.env.*
!.env.example

# The analysis cache and anything else written at runtime.
data/
data-localdev/
*.db
*.db-journal
*.db-wal
*.db-shm

*.log
.DS_Store

# Written by the deploy recipe on the server so a deploy can be rolled back.
.deployment-rollback-sha
